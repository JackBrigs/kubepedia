
!/projects/kubepedia/
!/projects/kubepedia/**
kubespray-src/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# Claude Code service files — never commit
.claude/
.claude/**
*.claude-session
.venv/

# upstream source caches (pinned-tag research, never committed)
src-cache/

# разобранный корпус базы для поиска — производное от kb/, пересобирается сам
.cache/

# журнал пробелов поиска: сырые запросы (могут содержать имена хостов)
reports/gaps.jsonl
reports/nightly/
