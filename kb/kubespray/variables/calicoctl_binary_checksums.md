---
id: VARIABLE-CALICOCTL_BINARY_CHECKSUMS
type: variable
title: calicoctl_binary_checksums
status: active
kubespray_version: ">=v2.27.0 <=v2.27.1"
kubernetes_version: null
component_version: null
verified_at: "2026-07-18"
confidence: verified
aliases:
  - calicoctl_binary_checksums
tags:
  - kubespray-defaults
  - variable
sources:
  - type: code
    path: roles/kubespray-defaults/defaults/main/checksums.yml
    url: https://github.com/kubernetes-sigs/kubespray/blob/v2.27.1/roles/kubespray-defaults/defaults/main/checksums.yml
    note: "default: (structured / block value — see source)"
relations: []
---
<!-- generated: variable-stub -->

# calicoctl_binary_checksums

## Summary

Kubespray variable `calicoctl_binary_checksums` — default `(structured / block value — see source)`. Defined in `roles/kubespray-defaults/defaults/main/checksums.yml`. Present in Kubespray
`v2.27.0`–`v2.27.1` of the indexed range. **Removed after `v2.27.1`** (absent in later tags of the range). (Source-grounded reference stub; see the file for
the exact value and surrounding context.)

## Implementation

Defined in `roles/kubespray-defaults/defaults/main/checksums.yml` (Kubespray `v2.27.1`):

```yaml
calicoctl_binary_checksums: (structured / block value — see source)
```

## Compatibility

Present in the Kubespray tags `v2.27.0`–`v2.27.1`. **Removed after `v2.27.1`** (absent in later tags of the range). Read the exact per-tag value from the source
file, as defaults can change between releases.

## References

- `roles/kubespray-defaults/defaults/main/checksums.yml` (Kubespray `v2.27.1`).
